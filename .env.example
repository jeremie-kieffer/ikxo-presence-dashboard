# Front — exposées au bundle Vite (préfixe VITE_ obligatoire).
# La clé anon/publishable est publique par nature : safe côté navigateur.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Scripts Python de migration — côté serveur uniquement.
# La service_role ne doit JAMAIS être exposée au front.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
